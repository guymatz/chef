import os
CURRENT_PATH = os.path.abspath(os.path.dirname(__file__).decode('utf-8')).replace('\\', '/')

DEBUG = True
TEMPLATE_DEBUG = DEBUG

ADMINS = (
    ('George Vilches', 'georgevilches@clearchannel.com'),
)

MANAGERS = ADMINS

DATABASES = {
    'default': {
        'ENGINE': 'django_pyodbc',
        'HOST': '<%= node[:partners][:db_host] %>',
        'NAME': '<%= node[:partners][:db_name] %>',
        'USER': '<%= @db_creds[:username] %>',
        'PASSWORD': '<%= @db_creds[:password] %>',
        'OPTIONS': {
            'driver': '/usr/lib64/libtdsS.so.2',
            'host_is_server': True,
            'USE_THIS_EXACT_STRING': 'Driver=/usr/lib64/libtdsS.so.2;Server=<%= node[:partners][:db_host] %>;UID=<%= @db_creds[:username] %>;PWD=<%= @db_creds[:password] %>;PORT=<%= node[:partners][:db_port] %>;DATABASE=<%= node[:partners][:db] %>;TDS_VERSION=8.0;',
        }
    },

    'auth_db': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': 'db.sqlite3',
    }
}
