

CACHES = {
  'default': {
    'BACKEND': 'ihr.cache.backends.memcached.IhrMemcachedCache',
    'LOCATION': [
                 <%= @memcached.map {|s| "'#{s}'"}.join(",") %>
                ],
    'KEY_PREFIX': 'W'
  }
}
