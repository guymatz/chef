// Generated by CoffeeScript 1.4.0
(function() {

  define(function(require) {
    var $, Backbone, ManagerView, Messages, run;
    $ = require('jquery');
    Backbone = require('backbone');
    ManagerView = require('src/browse/manager');
    require('src/browse/router');
    Messages = (function() {

      function Messages() {
        var _this = this;
        this.global = Backbone.View.prototype.global;
        this.global.on('notify', function(type, text) {
          return _this.message(text, type);
        });
      }

      Messages.prototype.message = function(text, type) {
        return $('.top-right').notify({
          type: type,
          message: {
            text: text
          }
        }).show();
      };

      return Messages;

    })();
    new Messages();
    return run = function() {
      $('#main').html(new ManagerView().render().el);
      return Backbone.history.start({
        silent: false,
        pushState: false
      });
    };
  });

}).call(this);
