import datetime

MONGOS = {
    'default': {
        'HOST': '<%= @news.mongo.join(',') %>',
        'DATABASES': {
            'default': {
                'COLLECTIONS': ['who'],
                'NAME': 'dfp',
            },
        }
    },
}
SECRET_KEY = '<%=@news.secret_key%>'
TTL = datetime.timedelta(hours=24)
TTL_WHERE_DFP_FORGETS_EVERYTHING = datetime.timedelta(hours=3)
DEFAULT_JUMP_URL = 'http://www.premiereradio.com/'
DEFAULT_IMAGE_URL = 'http://www.premiereradio.com/images/msthd3_new.jpg'
